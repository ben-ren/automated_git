This is a bash script that automatically runs git commands.
current version only pushes new projects and changes. 

To run input:

bash git_auto.sh new https://github.com/ben-ren/automated_git.git main "Initial commit"

OR

bash git_auto.sh update main "commit"

TODO: 
- Make script executable
- Add script to PATH, to allow script to be run from anywhere